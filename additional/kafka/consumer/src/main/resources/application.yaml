spring:
  application:
    name: consumer
  cloud:
    function:
      definition: processRiderLocation;processRiderStatus
    stream:
      bindings:
        processRiderLocation-in-0:
          destination: my-topic
          group: rider-location-grp
        processRiderStatus-in-0:
          destination: new-topic
          group: rider-status-grp
    kafka:
      binder:
        brokers: localhost:9092

#  kafka:
#    bootstrap-servers: localhost:9092
#    consumer:
#      group-id: my-group
#      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
#      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
#      properties:
#        spring.json.value.default.type: com.demo.consumer.RiderLocation
#        spring.json.trusted.packages: com.demo.consumer,com.demo.producer
#        spring.json.use.type.headers: false

server:
  port:
    8081